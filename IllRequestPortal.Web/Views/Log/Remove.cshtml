@* --------------------------------------------------------------------*@
@* Warning! This is an auto generated file. Changes may be overwritten *@
@* --------------------------------------------------------------------*@
@using Localization
@using Microsoft.Extensions.Options
@inject LocService SharedLocalizer
@model IllRequestPortal.Web.ViewModel.LogViewModel

@{
    ViewData["Title"] = $"{SharedLocalizer.GetLocalizedHtmlString("Delete")} {SharedLocalizer.GetLocalizedHtmlString("Log")}";
}

<h1>@($"{SharedLocalizer.GetLocalizedHtmlString("Delete")} {@SharedLocalizer.GetLocalizedHtmlString("Log")}")</h1>

<h3>@SharedLocalizer.GetLocalizedHtmlString("Are you sure you want to delete this?")</h3>
<div>
    <hr />
    <dl class="row">

        <dt class = "col-sm-2">
            <label asp-for="Origin" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Origin")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Origin
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="Message" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Message")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Message
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="LogLevel" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("LogLevel")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.LogLevel
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="CreatedOn" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("CreatedOn")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.CreatedOn
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="Exception" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Exception")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Exception
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="Trace" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Trace")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Trace
        </dd>
    </dl>
    <form method="post"  enctype="multipart/form-data" asp-controller="Log" asp-action="Remove">    
        @Html.HiddenFor(x => x.Id)
        <input type="submit" value="@SharedLocalizer.GetLocalizedHtmlString("Delete")" class="btn btn-danger" /> |
        <a asp-controller="Log" asp-action="Index">@SharedLocalizer.GetLocalizedHtmlString("Back to List")</a>
    </form>
    </div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

